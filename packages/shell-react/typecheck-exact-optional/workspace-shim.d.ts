/** Minimal declaration boundary used by the emitted React command UI types. */
export interface WorkspaceFile {
  content: string;
  mimeType?: string;
  path: string;
  source?: string;
  updatedAt?: string;
}
