/** Phase 0 placeholder — WorkbenchShell and providers arrive in a later phase. */
export const WORKBENCH_KIT_WORKBENCH_REACT_VERSION = '0.0.0' as const;

export type WorkbenchShellPlaceholderProps = {
  readonly children?: never;
};
