{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://workbench-kit.dev/schemas/recursive-architecture-research-result.schema.json",
  "title": "Recursive architecture research result",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "taskKey",
    "controlSha",
    "baseSha",
    "status",
    "lane",
    "question",
    "verdict",
    "evidence",
    "nextQuestions",
    "validation",
    "nextAction"
  ],
  "properties": {
    "schemaVersion": { "const": 1 },
    "taskKey": { "const": "hourly_project_steward" },
    "controlSha": { "type": "string", "minLength": 7 },
    "baseSha": { "type": "string", "minLength": 7 },
    "status": {
      "enum": [
        "NO_ACTION",
        "RESEARCH_COMMITTED",
        "PLAN_UPDATED",
        "DOCS_UPDATED",
        "HANDOFF_REQUIRED",
        "INTERACTIVE_VALIDATION_REQUIRED",
        "VALIDATION_FAILED",
        "COOLDOWN_REQUIRED",
        "BLOCKED"
      ]
    },
    "lane": { "type": ["string", "null"] },
    "question": { "type": ["string", "null"] },
    "verdict": {
      "enum": ["maintain", "learn", "evolve", "improve", "retire", "not_applicable", null]
    },
    "evidence": { "type": "array", "items": { "type": "string" } },
    "nextQuestions": { "type": "array", "items": { "type": "string" } },
    "workItemKey": { "type": ["string", "null"] },
    "ownership": {
      "enum": ["generic", "host_policy", "composition", "not_applicable"]
    },
    "commit": { "type": ["string", "null"] },
    "validation": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["command", "status"],
        "properties": {
          "command": { "type": "string" },
          "status": { "enum": ["passed", "failed", "not_run"] },
          "reason": { "type": ["string", "null"] }
        }
      }
    },
    "nextAction": { "type": "string" }
  }
}
